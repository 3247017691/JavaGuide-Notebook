---
name: JavaGuide 离线小抄
description: 暖亚麻案面上一摞过塑索引卡——按篇幅定版面，读一篇涂一格，整章读完盖背完章
colors:
  desk: "#e7e0cd"
  desk-2: "#dbd3bf"
  desk-deep: "#c7bda4"
  desk-ink: "#565044"
  desk-ink-2: "#6f6859"
  paper: "#f9f6ee"
  paper-2: "#f0e9d8"
  paper-read-2: "#f6f0e3"
  paper-edge: "#dcd3be"
  paper-back: "#e2d8c0"
  paper-fold: "#c9bfa2"
  paper-toc: "#fbf8f1"
  punch-line: "#c3b89c"
  ink: "#17181a"
  ink-soft: "#3a3d3b"
  ink-faint: "#5f625d"
  red: "#b0492f"
  red-deep: "#8c3520"
  hi: "#f2e8a0"
  tip-bg: "#f6e3c8"
  danger-bg: "#f3d9d2"
  info-bg: "#dde8e2"
  scroll-thumb: "#b3aa95"
  scroll-thumb-hover: "#9a917c"
  toc-scroll-thumb: "#cfc7b2"
  toc-scroll-thumb-hover: "#b9b09a"
  ch-01: "#7a4fae"
  ch-02: "#2f6db0"
  ch-03: "#23703f"
  ch-04: "#8a5a1f"
  ch-05: "#1f6e6e"
  ch-06: "#a04818"
  ch-07: "#9a2f6d"
  ch-08: "#5a6e2f"
  ch-09: "#55606a"
  ch-10: "#2f4f8a"
  ch-11: "#8a2fb0"
  ch-12: "#8a4a7a"
  code-bg: "#fbf8f1"
  code-fg: "#17181a"
  code-edge: "#dcd3be"
  code-thumb: "#b3aa95"
  code-kw: "#0000ff"
  code-type: "#1f6b80"
  code-fn: "#795e26"
  code-str: "#a31515"
  code-num: "#0a7a50"
  code-cmt: "#008000"
  code-prop: "#0451a5"
  code-var: "#001080"
  code-tag: "#800000"
  code-sym: "#811f3f"
  code-add: "#045104"
  code-del: "#a31515"
  code-chip: "color-mix(in srgb, var(--ch) 10%, var(--paper-2))"
  desk-vignette: "rgba(88, 74, 44, .10)"
  sheen: "rgba(255, 255, 255, .55)"
  track: "rgba(23, 24, 26, .2)"
typography:
  display:
    fontFamily: "Kaiti SC, KaiTi, STKaiti, 楷体, cursive"
    fontWeight: 700
  band:
    fontFamily: "PingFang SC, Microsoft YaHei, 微软雅黑, Heiti SC, sans-serif"
    fontWeight: 800
  body:
    fontFamily: "Songti SC, SimSun, 宋体, Source Han Serif SC, serif"
    fontWeight: 400
    fontSize: "16px"
    lineHeight: 1.8
  read:
    fontFamily: "Songti SC, SimSun, 宋体, Source Han Serif SC, serif"
    fontWeight: 400
    fontSize: "16.5px"
    lineHeight: 1.92
    measure: "40em"
    sizeSteps: "15.5 / 16.5 / 17.5px（--fs，html[data-size=s|m|l]）"
  mono:
    fontFamily: "Cascadia Mono, Consolas, Courier New, monospace"
    fontSize: "11.5-13.5px"
rounded:
  tick: "0.5px"
  chip: "1px"
  inline: "2px"
  control: "3px"
  card: "4px"
spacing:
  cell: "14px 14px 13px"
  deck-gap: "clamp(14px, 1.5vw, 20px)"
  section: "clamp(22px, 2.8vw, 36px)"
components:
  card:
    backgroundColor: "{colors.paper}"
    textColor: "{colors.ink}"
    rounded: "{rounded.card}"
  stamp-btn:
    textColor: "{colors.red-deep}"
    rounded: "{rounded.control}"
  btn:
    backgroundColor: "{colors.paper}"
    textColor: "{colors.ink-soft}"
    rounded: "{rounded.control}"
---

# Design System: JavaGuide 离线小抄

## Overview

**Creative North Star: "The Laminated Cheat Sheet（过塑小抄）"**

整个产品是一摞摊在暖亚麻案面上的过塑索引卡：12 章 = 12 张卡，332 篇 = 卡上的 332 道刻度。纸是唯一的表面。**版面由篇幅决定**——70 篇的 Java 拿 6×2 格，8 篇的高可用拿 2×1 格，一眼看得出哪几章最厚。墨三阶锁死（ink / ink-soft / ink-faint）；朱砂只属于"已读/划掉"；荧光笔只给当前项与 ::selection；12 支章色降级为卡顶 3px 色线与编号，不再是大面积色块。

**Key Characteristics:**
- **两级进度，一处操作三处一致**：进度有两个刻度——「篇」（整篇盖章）和「节」（阅读页悬浮目录里逐节划掉）。两者**双向联动**，规则由服务端统一裁决：一篇的小节**全部**划掉会自动盖上整篇「已读」章，退掉任意一节会自动撤章；反过来整篇盖章会把这篇所有小节一起划掉，撤章则一起清空。所以无论从首页、章节页还是阅读页操作，三处讲的都是同一件事。（`read_marks` 与 `section_reads` 仍是各自的事实来源，联动只发生在写操作上，且在启动时对账一次，补齐规则上线前的历史孤岛。）
- 折角即进度：卡片右上角是一只折过来的"狗耳朵"，`--foldpx` 随完成率从 18px 长到 60px；折角画成纸背（paper-fold），带折痕投影。
- 刻度阵即格：卡底一排细竖线，一篇一道；已读落墨（ink），**读了一部分按小节完成率从下往上涂成微条**，未读是章色三成的淡痕。两行高的主课卡把刻度放大到两排，让"篇幅"占满版面。
- 三处同步：已读 = 穿孔圈落「阅」+ 波浪朱线 + 刻度涂墨，首页则是刻度涂墨 + 朱砂进度尺 + 背完章。**「读了一部分」是第三个状态**：穿孔只打虚线半圈、朱线**只画一半**（长度 = 小节完成率）、刻度上是半截微条——形状先说话，颜色只作次要信号。
- 四种笔各司其职：楷体（手写：报头/章名/计数/印章/批语/`.md-c` 容器标题）、黑体（卡名/表头/按钮/**正文区 h2–h4**）、宋体（正文）、等宽（登记数字）。一笔一职的原则不变，只交接了一职：正文区标题从宋体族 + 超真实字重改为黑体族 700——中文衬线族普遍没有 700/900，写上去只会得到描边造假粗，久看边缘发糊。

## Colors

三色纪律 + 12 支马克笔。**The Ink-Only-Three Rule**：墨色只有三阶，任何文字不许发明第四种灰。**The Pen-Red Rule**：朱砂只写"已读/划掉"，永不做按钮底色或链接常态色。

### Primary
- **朱砂** (#b0492f / #8c3520)：进度尺填充、波浪朱线、穿孔「阅」字、背完章、完成率数字。
- 案面上的朱色文字（报头那个分隔点、错误提示链接）一律用 #8c3520 —— #b0492f 在案面上只有 3.7:1。

### Secondary
- **荧光笔** (#f2e8a0)：目录当前项底色、hover 底、::selection。

### Tertiary（12 支马克笔，public/colors.js）
- 01 紫 #7a4fae · 02 蓝 #2f6db0 · 03 绿 #23703f · 04 琥珀 #8a5a1f · 05 青 #1f6e6e · 06 橙 #a04818 · 07 品红 #9a2f6d · 08 橄榄 #5a6e2f · 09 石墨 #55606a · 10 藏青 #2f4f8a · 11 紫罗兰 #8a2fb0 · 12 兰紫 #8a4a7a
- 全部 12 支在纸上都 ≥4.5:1，可安全用作编号与卡名旁的小字。章色只出现在：卡顶 3px 线、inset 顶线、编号、极淡纸染（7%）、刻度未读态（32%）、目录便签底（8%）。

### Neutral
- **案面** (#e7e0cd→#dbd3bf→#c7bda4 径向) + 极细亚麻织纹（fixed 覆盖层，4px 周期，纵向 2.2% / 横向 1.4%，`--linen-a` / `--linen-a2`）+ 边缘压暗（`--vignette-a` .10）。织纹与暗角的强度照「盯四十分钟」定，不照「一眼惊艳」定：高频细纹理落在周边视野里会持续勾走注意力。三者全部走 token，所以夜读只改值、`≤760px` 只关层，不新增选择器。案面上的字取 --desk-ink (#565044，5.6:1)。
- **纸** (#f9f6ee→#f0e9d8)：所有卡面；纸边 #dcd3be 做虚线与描边；折角露出的纸背 #c9bfa2→#e2d8c0。**阅读正文这面纸单列一档终点**（`--paper-read-2` #f6f0e3，台阶 1.121→1.052）——1.12 的渐变在一张小卡上是纸感，在一整页滚动的正文里是背景漂移，眼睛要跟着重新调曝光。
- **墨三阶** (#17181a / #3a3d3b / #5f625d)：正文 / 次级 / 弱级，三阶在纸上都 ≥5.6:1。

## Typography

**Display / Section Font:** 楷体（圆珠笔手写声：报头标题、章名、计数、印章、批语、`.md-c` 容器标题）
**Band Font:** 黑体（卡名、分组标题、按钮、表头、**正文区 h2–h4** —— 印刷表头的声音）。分组标题字重 900 → 700：系统黑体最粗只到 Bold(700)，写 900 等于要求浏览器描边造假粗；正文区标题同样锁 700 并加 `font-synthesis-weight: none` 防回归。卡名/表头其余处的 800 暂留（方案未点名），属已知偏差。
**Body Font:** 宋体（正文 16.5px / 1.92，**行长 40em 单栏**）。justify 只给段落，标题、列表项、代码块一律左对齐——短行被拉成两端对齐时词间空隙会白成一条河；CJK 配 `text-justify: inter-ideograph`，行长不足时（`≤600px`）整体退回左对齐。
**Label / Mono Font:** Consolas（编号、时间戳、百分比、页脚）

### Hierarchy
- **Masthead** (700, clamp 27-41px, .07em tracking)：报头「JavaGuide · 离线小抄」。
- **Score** (700, clamp 26-34px, tabular-nums)：总评大数字；分隔符 15px、分母 20px。
- **Card name** (800)：主课卡 clamp 20-26px、中卡 clamp 15.5-18px、窄卡 15px。
- **Read title** (700, clamp 24-34px)：文章标题。
- **Body** (400, 16.5px/1.92)：正文单栏 40em（≈40 个汉字），字号由 `--fs` 三档驱动。
- **Article headings** (黑体 700)：h2 19.5px + 2.5px 章色下边线、h3 与正文同字号、h4 15.5px。层级靠「字族差 + 勾线」承担，不靠堆字号与字重——标题与正文只差字号时，读者要靠猜。
- **Label** (mono 11.5-13.5px)：编号、时间、百分比、页脚。

## Layout

**首页 bento（12 列 × 5 行）**：章节顺序不变，格子按篇幅给。`--col` / `--row` 由固定表给出，5 行每行正好填满 12 列，不留空洞：
01(2×1) 02(6×2) 03(4×2) 04(2×1) 05(4×1) 06(2×1) 07(2×1) 08(4×1) 09(2×1) 10(6×2) 11(4×2) 12(2×1)。
`≤1080px` 放弃 bento 退回两列等宽；`≤760px` 单列。骨架卡与真卡共用同一套 `--col/--row`，加载前后不跳版。

**章节卡页**：单张 920px 纸面，色带降级为 4px inset 顶线 + 编号，分组标题 3px 章色下边线；行高 2px 墨线下沿被朱砂按完成率填掉。

**阅读页**：`grid: var(--toc-w) minmax(0,1fr)`，目录卡在左（sticky，`max-height: calc(100vh - var(--pad-y)*2)`），纸面在右，总宽 min(1010px + toc + 26px, 100vw-28px)。`≤1024px` 整页塌成 block，退回"正文在上、目录在下"。目录头（`.toc h2`）下沿有一条 1.5px 墨线，**被朱砂按小节完成率从左填掉**（`scaleX(--p)`），与章节页那条 2px 线是同一种语言；旁边标 `n/m 节`，全划完转朱色（`.toc-done`）。
`≥1025px` 时版面多出第三列 `--rail-w: 40px` 给右侧导航（见「右侧导航轨」）——**导轨位置由版面算出，不手写偏移量**：正文限宽 40em、纸面 1010px，纸面右侧那条空白正好是它的位置，纸面随窗口怎么变它都贴着纸边。版心用 `em` 而不用绝对像素，是让「换字号档」时行长恒定 40 字——只有字在长大，行的形状不变。

## Elevation & Depth

一张纸离桌一次投影，无分层。**The One-Lift Rule**：纸面只投影一次。
`clip-path` 会把 `filter: drop-shadow` 的输出一起裁掉，所以投影必须挂在**外层 `<a>` / 容器**上，裁剪挂在**内层 `.card-paper`** 上——这是全站折角卡的结构约定。
折角自己再带一层 `drop-shadow(-1px 1px 1.4px)` 当折痕投影。

## Shapes

折角是签名轮廓：`clip-path` 折角，折过来的纸背填 `--foldpx` 正方形，`clip-path: polygon(0 0,100% 100%,0 100%)`，折痕沿裁剪边的对角线。
控件圆角 ≤4px，刻度 0.5px、小片 1px、控件 3px、纸面 4px。穿孔是 26px 正圆（虚线=未读，实线朱=已读）。无大圆角、无玻璃、无渐变文字。

## Components

### 卡（.card / .card-paper / .fold）
纸渐变 + 章色 inset 顶线 + 折角 + 刻度阵 + 计数 + 细进度语义；hover 抬起 **2px**（原 5px——这是摊在桌上的纸片，不是可点的浮层，抬 5px 是在承诺一个它给不了的层级）、掠出过塑反光、投影加深；`data-rows="2"` 的主课卡内容垂直居中、刻度放大、计数 30px。整章读完右下落「背完」朱章（-9°，scale 2.2→1 弹入），百分比让位不再重复。

### 总评刻度尺（.tape）
12 段，`flex-grow` = 该章篇数（段宽即篇幅），段内朱砂填充 = 该章完成率。这是"首页任何数字都必须与库中状态实时一致"的最小仪表盘。

### 刻度三态（.card-ticks i / i.on / i.mid）
未读 = 章色 32%；已读 = 纯墨；**读了一部分 = `linear-gradient(to top, 墨 var(--f), 章色32%)`，`--f` = 该篇小节完成率**——一道从下往上涂到一半的微条。三态共用同一套刻度，所以"篇幅"与"进度"是一张图。
刻度宽 4px（原 3px：在 12px 高的小竖线上，一像素之差就是"看得见"与"糊成灰条"），两行主课卡放大到 6px、`≤1080px` 收到 4.5px（必须仍宽于 4px 基准，否则"放大档"反而比常规细）。仍然是一篇一道的**刻度**，不是进度条——条状语义留给 `.tape` 与目录头那条朱线。

### 小节进度（.card-secs / .entry-secs / .score-secs / .toc-prog）
阅读页悬浮目录里划掉的小节，在这里汇总成数。**只在真有"半读篇"时才渲染 `.card-secs`**（否则它与上面的篇数讲的是同一件事，纯属重复）；`≤760px` 的窄卡与紧凑处不显示。章节页每行 `.entry-secs` 报 `n/m 节`，只在 `n>0` 时有内容（空壳 `.is-off` 留在 DOM 里，标记已读时就地填上，不重绘整行）。**不做进度条**：全章小节基数上千，0.5% 的条看起来是空的，反而像坏了。

### 章卡目录行（.entry）
整行是一条链接；穿孔圈（已读落朱「阅」字）+ 标题 + 小节进度 + 时间戳；右侧独立按钮只管「标记已读 / 取消已读」。**朱线长度 = 小节完成率**——挂在 `.entry-title::after` 上，用 `clip-path: inset(0 calc((1 - var(--secp)) * 100%) 0 0)` 从右往左揭出。读了一半就只画一半，整篇读完才是完整的一道波浪。（旧版是整条背景图 + 滑入动画，画不出"半条"，所以改造。）`≤760px` 时小节进度掉到标题下一行（`grid-column: 1 / -1`），挤在第三列会把纸面顶破。

### 宽表格（.md-table-wrap）
正文里 markdown 渲染出的是裸 `<table>`，窄屏会顶破纸面。`read.js` 注入正文时就地包一层可横滑的壳；外边距从表格挪到壳上，桌面端表格外观一字未变。
表内**只留横线**：14px 字号、单元格 `padding: 8px 12px`、每行一条 `--paper-edge` 细线，竖线全去、表头不铺底色而靠 2px 墨线 + 700 字重分离。八股对比与参数速查这类密表原本横竖全框，一张表就是一道视觉栅栏，读者要横穿六条线才能对上列。

### 右侧导航轨（.rail / .rail-btn）
阅读页右侧留白里的一摞圆形按钮：**回桌面 / 回上一级 / 回最顶端**，其下隔 9px 是两颗阅读偏好——**夜读切换**（太阳与月亮同一颗按钮换脸，`aria-pressed` 记状态）与**字号三档**（一颗写实的 `A`，按当前档位缩放，档位即所见）。导航与偏好共用同一套按钮样式，只靠那 9px 间距分组，不另发明一种控件。
圆是「穿孔」在全站一贯的形状语言，图标是一笔画 SVG（房子 / 上一层箭头 / 箭到顶线 / 日月 / A），不用字符或 emoji。
- **版位靠版面算，不靠偏移量**：`≥1025px` 是 `.read-sheet` 的第三列（`grid-area: 1/3` + sticky），永远离纸边 26px；`<1025px` 那条留白会被正文吃掉（正文自己顶到纸边），改成右下角一小摞 —— 宁可压住角落，也不为了放按钮去收窄正文。两种版位共享一套按钮样式，媒体查询只改「怎么定位」。
- `position: fixed` / `sticky` 的 grid 子元素都不占轨道，所以导轨不会把 grid 挤出隐式列（这是本仓踩过的坑）。
- 已经在顶端时「回顶端」用 **opacity 收起而不是 display** —— 导轨高度不变，另外两个按钮就不会跟着跳。
- 小控件只配收紧的投影（`0 4px 9px -4px`）：纸面才配那一次大 lift，1.5px 细边配 18px 大模糊会被检测器判成 "thin border + wide shadow"。
- **入场只淡入**（`@keyframes` 里不写 transform）—— 两种版位的 transform 不同，塞进 keyframes 会互相覆盖，也会和 `prefers-reduced-motion` 的 `animation: none` 打架。悬停仍可抬起 2px（那是 `:hover` 上的 transform，与入场无关），`prefers-reduced-motion` 下连同 `:active` 一起取消。「回顶端」的平滑滚动也读 `prefers-reduced-motion`。
- **悬停与聚焦用墨不用朱**：导轨是「位置」控件，不是「批注」控件。反馈 = 墨色加深（`--ink-soft`→`--ink`）+ 纸面被灯照到（`--paper-hi`）+ 那一次 2px 抬起，不新造色值。

### 印章按钮（.stamp-btn）
阅读页右上，未读=纸底墨字，已读=朱深底白字 + 内白描边，-2° 旋转。

### 容器（.md-c）
tip=荧光底、warning=橙纸底、danger=红纸底，楷体粗题，无侧条。

### 代码块（.md-pre）
白昼：`--code-bg` = `--paper-toc`（#fbf8f1，系统里最浅的那层纸——与 mermaid 配图、便签目录同一支纸），`--code-edge` 1px 细边收住边界（浅底对浅纸只有 1.02～1.07 的台阶，光靠明度分不出这块纸），再加章色顶边 3px + 柔投影。文字 `--code-fg` = `--ink`，16.75:1。等宽 13.5px / 1.75。**不倾斜**——-.22° 的手写感属于卡片轮廓，压在等宽文本上只会让字形亚像素发虚。
夜读：整块翻回**抬面** `#26241e`（比纸 #1b1a16 亮一档、台阶 1.12），文字 `#e8e6dd` 12.4:1，`--code-edge` 收成 `transparent`——夜里明度台阶已经够了，描边只会多出一道轮廓噪点。
两种模式下纸→块台阶都 ≤1.12，远低于 ≤11:1 的上限：一屏代码不该像闪了次黑屏，也不该比正文纸更抢注意力。

**语法色板是两套，不是一套**：highlight.js 的标记全部走 token——`--code-kw / -type / -fn / -str / -num / -cmt / -prop / -var / -tag / -sym / -add / -del` 十二支。白昼取 VS Code 现代浅色（Light Modern）的判定色，夜读在 token 块里覆盖回原来那套纸感暖调（#ffd75e / #9fd3a0 / #e8a08f / #8fc6ff…）。同一套色板不可能同时活在浅色纸和暗纸上：浅纸要的是一串蓝绿赭红，暗纸要的是暖黄青绿，共用一边可读一边难看。`operator`、`punctuation`、`params`、`meta` **不设 token**，跟着 `--code-fg` 走墨色——结构性的中性符号不该跟语义色抢注意力（VS Code 浅色主题里它们同样是前景色）。
白昼 12 支逐一算过（对 #fbf8f1）：kw 8.10、str 7.40、var 14.29、prop 7.26、tag 10.32、fn 5.75、type 5.70、num 5.06、sym 8.99、add 9.07、del 7.40、cmt 4.84，全部 ≥4.5:1 AA。**三处偏离 VS Code 原值是有意的**：类型 #267f99→#1f6b80（原值只有 4.32）、数字 #098658→#0a7a50（4.34）——这两支在 VS Code 自己的纯白上就贴着 AA 线，我们的纸是暖白 #fbf8f1、比纯白暗一档，照搬会掉到线下；预处理器灰 #808080（3.72）不入板，改跟墨走。
顺手补上 `.hljs-attribute`（全库 126 处）——旧规则只写了 `.hljs-attr`，HTML/XML 的属性名一直没上色，一直靠继承混在正文墨里。

### 行内 code 贴片（.article code:not(pre code)）
底色走 `--code-chip`，不是散值。白昼 = 章色 10% + `--paper-2`，比纸面**暗**一档，贴片读作"压深的墨痕"；文字沿用正文的 `--ink`，12 支章色逐一算过，最不利 12.65:1。
夜读**不能沿用同一条混合**——四层纸在夜里是翻面的（`--paper-2` #1f1e19 比正文纸 #1b1a16 **亮**），沿用会把"暗一档"变成"亮一档"，而贴片上的字还是那支墨：12 支章色里 9 支掉到 11:1 以下，最不利 10.76:1。夜读改成章色 6% + `--paper-toc`（#191813，夜里最暗的那层纸），关系与白昼同向，最不利 11.91:1。
大小两块的**表面语义不同**，别把它们算作同一回事：小贴片是"同一张纸上的墨痕"，靠压深一档找到；整块代码是"另贴一张纸进来"，白昼贴更浅的那支纸（#fbf8f1）+ 细边 + 章色顶边，夜读没有更浅的纸可贴、只能往上抬（#26241e），因为它自带更亮的 `--code-fg` #e8e6dd，抬起来仍有 12.4:1——而贴片继承正文的墨，抬起来第一个吃亏的就是眼睛，所以夜里它反而要往下挖。

### 状态
骨架卡（.is-skeleton，与真卡同格位同尺寸的 shimmer）→ 有数据；API 失败给成句的 `.deck-note` / `.sheet-note`（说明问题 + 回去的路），不是一行红字。

## 阅读偏好：夜读与字号

只有两项偏好，因为它们直接对应疲劳；其余都属于个性化噪声（不做主题商店）。

**The Token-Only Rule（夜读）**：夜读**只覆盖 token，不新增选择器**。两套覆盖块——`html[data-mode="night"]`（手动）与 `@media (prefers-color-scheme: dark){ html:not([data-mode="day"]) }`（未手动选时跟随系统）——**必须逐字同值**（现 64 条）。漏一条就是一道没暗下来的白缝，足以毁掉整页的暗适应，而这类漏项在截图评审里看不出来。任何一处改动都要同时改两块。
- 三态由 `read-mode` 记在 localStorage：`day` / `night` / 未设置（=跟随系统）。`<head>` 内联一小段 bootstrap 在 CSS 落地前贴好 `data-mode`，避免闪白（FOUC）。
- 纸面最暗只到 #121110，**绝不用纯黑**；同理夜读里不许出现纯白表面。逐屏要查的漏项高发地：图片内衬、mermaid、目录当前项荧光底、滚动条、toast、过塑反光。
- 关键值：正文纸 #1b1a16（渐变终点 `--paper-read-2` #1f1e19，台阶 1.043）、墨 #ded8c8（纸起点 **12.24:1**，渐变终点 11.73:1，全程 ≥11）、代码块底 #26241e（比纸**略亮**一档、台阶 1.12，抬面而非挖洞，其上文字 #e8e6dd 12.4:1，`--code-edge` transparent、滚动条贴片回 rgba(242,232,160,.35)，十二支语法色一并覆盖回纸感暖调——白昼那套 VS Code 浅底色在暗纸上最高的只有 4.3:1，会整块糊掉）、行内 code 贴片 `--code-chip`（章色 6% + 最暗纸层 #191813，比纸**略暗**一档——夜里纸层翻面，贴片必须跟着翻向，否则 9 支章色不达 11:1）、荧光底 #4d4526、`--cast: 0,0,0`。夜读 `--ink-faint` #8d8779 仍有 4.87:1，弱级墨不退到不可读。
- **章色靠混合比让路**：正文链接色 = 章色与墨按 `--ch-mix` 混合，白昼 82%（章色主导），夜读压到 58% 让墨色托住对比度；悬停档 `--ch-mix-hov` 60% / 48% 章色更少、更靠近墨。混合百分比也是 token，所以换模式不用换规则。12 支章色逐一算过，**一律取最不利的那一端**（夜读对渐变终点 #1f1e19、白昼对 #f6f0e3）：夜读常态 ≥4.71:1、悬停 ≥5.58:1；白昼常态 ≥5.89:1、悬停 ≥7.91:1——四态全部过 AA；短板每套各一支——白昼最低是 02 蓝 #2f6db0（常态 5.89:1），夜读最低是 10 藏青 #2f4f8a（常态 4.71:1）。
- 案面织纹在夜读换成黑线（`--linen: 0,0,0`、`--linen-a` .22、`--linen-a2` .14），暗角整层关掉（`--vignette-a: 0`）。alpha 从 2.2% 跳到 22% 不是笔误：台阶要看的是压暗后的**比值**——白昼 2.2% 棕线把 #e7e0cd 压到 #e4ddca（1.029:1），夜读 22% 黑线把 #121110 压到 #0e0d0c（同样 1.029:1）。底越暗，同样的感知台阶需要越高的 alpha；数值差十倍，感知等量——桌子还是桌子。
- `color-scheme: light` + 夜读侧 `dark`，让原生控件（滚动条、表单、`::selection` 底）跟着换调，不额外画第二套。
- **mermaid 跟随夜读**：`theme: night ? "dark" : "neutral"`；切换时从 `dataset.src` 取原始 markdown 复原 DOM 再重渲染（`removeAttribute("data-processed")`），并防抖 120ms。

**字号三档**：`--fs` = 15.5 / 16.5 / 17.5px，挂在 `html[data-size=s|m|l]`，记在 `read-size`。因为版心与内衬全部 `em` 化，换档时**行长恒定 40 字**，只有字在长大。浏览器 200% 缩放因此不需要横向滚动——这条不通过就说明还有漏网的像素值。

## Do's and Don'ts

### Do:
- **Do** 让版面表达篇幅：格子大小、刻度数量、折角大小三处同源。
- **Do** 用形状记号表达状态：折 / 涂 / 穿 / 盖 / 填，颜色只作次要信号。
- **Do** 让进度只有一处事实来源、多处呈现：写操作一律经服务端联动，前端只负责把结果画成形状。任何"局部进度"都不许成为孤岛。
- **Do** 进度动效走 transform（`scaleX`）或 `clip-path`，不走 width。
- **Do** hover 反馈统一：抬起 + 归正 + 反光掠出。
- **Do** 案面上的文字换用案面墨，不照搬纸上的灰阶。
- **Do** 让长读参数优先于氛围参数：新增任何一层质感之前，先回答它让「四十分钟后」更累还是更不累。质感要仍可感知，但要退出注意力竞争。
- **Do** 表面色的台阶算出来再定：纸→代码块 ≤11:1、渐变终点台阶 ≤1.06——一屏代码不该像闪了次黑屏，一整页正文不该让眼睛跟着背景重新调曝光。

### Don't:
- **Don't** 引入第四种墨灰、第二层纸面阴影、大圆角或玻璃拟态。
- **Don't** 把朱砂用作普通强调色或链接常态色，**也不做任何"位置类"控件的悬停色与聚焦轮廓色**（P1-12：红笔一旦当鼠标指针用，真正被圈出的重点就失效了）。范围包括正文链接、面包屑、目录展开/收起箭头与「全部展开」、页脚上一篇/下一篇、右侧导轨按钮——它们的悬停一律靠「墨色加深 + `--paper-hi` 灯照面 + 2px 抬起」表达，不引入新色值。朱砂的常态职位仍是四处：进度尺、波浪朱线、穿孔「阅」字、背完章；**只有「标记已读」类控件**（`.entry-toggle` / `.stamp-btn` / `.toc-mark`）的悬停红是批注语义本身，予以保留。
- **Don't** 在窄基准（全章上千节）上画进度条——0.5% 看起来就是坏的，改用数字。
- **Don't** 把投影挂在带 `clip-path` 的同一元素上（会被裁掉）。
- **Don't** 用 emoji / 图形字符充当图标（★ 为源文本内容除外）。
- **Don't** 引入外部字体、CDN 或任何运行时网络依赖。
