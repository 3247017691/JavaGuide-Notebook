# 面试工作台 · Vue3 重构还原方案（依据 参考/ 四张 macOS 27 实拍图）

> 状态：后端模块化已完成（`server/` 8 个文件）、`client/` 脚手架已建、依赖已装（vue3.5 / vite6 / element-plus2.9 / pinia2.3 / vue-router4.5）。
> 本文是前端部分的完整还原规格：把 `参考/` 四张图逐张翻译成设计令牌、组件规格与实施清单。
> 原则：**外壳走液态玻璃；小抄三页保持纸面世界（DESIGN.md 纪律不破）**。

---

## 一、四张参考图 → 还原对象映射

| 图 | 内容 | 还原为 |
|---|---|---|
| `LaunchOS_main-interface` | 启动台主界面：全屏模糊壁纸 + 顶部搜索胶囊 + 7 列图标网格 + 页点 + 玻璃 Dock | **启动台 overlay**（`Launchpad.vue`）：列出 2 个应用 + 12 章速捷瓷贴，搜索框实时过滤，页点留位 |
| `LaunchOS_folder` | 「Utilities」文件夹展开：居中标题 + 一整块大圆角玻璃面板内装 7×3 网格 | **文件夹玻璃面板**（`FolderPanel.vue`）：题库 14 个「检查系统」以文件夹瓷贴进入，展开为玻璃面板网格 |
| `apps-app_macOS27` | Applications 浏览窗：标题栏 = 应用小图标 + 可编辑标题 + 「⋯」；分段控件胶囊行；网格分区 hairline | **窗口标题栏升级**（图标 + 标题 + ⋯）；**分段控件**胶囊化（显示偏好 / 启动台过滤用）；分区 hairline 分隔 |
| `macOS27.jpg` | Preview 窗口：红绿灯与工具栏同排；**玻璃右键菜单**（顶部命名区、勾选态、子菜单箭头、分隔线）；菜单栏右侧状态簇 | **右键菜单规格**（CtxMenu）；**红绿灯悬停显形**；菜单栏右侧状态簇（进度芯片 / 外观 / 时钟） |

## 二、全局设计令牌（`client/src/styles/desk.css`）

### 2.1 玻璃材质（Liquid Glass 三档）
```css
--glass-thin:  rgba(255,255,255,.52);  /* 菜单栏 / Dock / 侧栏 */
--glass-med:   rgba(255,255,255,.66);  /* 窗口主体 */
--glass-thick: rgba(255,255,255,.82);  /* 弹层：菜单 / 面板 / Sheet */
--glass-dark:  rgba(28,26,24,.55) 系（深色模式逐档换）
backdrop-filter: blur(28px) saturate(1.6);   弹层加 blur(40px)
--glass-brd: rgba(255,255,255,.55) 内描边 1px + 外侧 0.5px 暗描边
[data-desk-opaque="1"] → 三档全部退成实面（blur 去掉，背景不透明）
@media (prefers-reduced-transparency) 自动降级同上
```

### 2.2 圆角体系（统一刻度，参考图实测比例）
```
窗口 20px · 大玻璃面板(启动台文件夹/Sheet) 24px · 菜单/右键 10px ·
控件(按钮/输入/分段) 8px · 胶囊搜索 999px · 应用图标 squircle 22.5%
```

### 2.3 色彩
- 浅色：文字 `#1d1d1f` 主 / `rgba(60,60,67,.6)` 次；强调色沿用各应用 accent（小抄朱砂 #b0492f / 题库 #ff6a3d），系统交互蓝仅用于链接与选中 `#0a60ff`
- 深色：面 `#1c1a18` 系玻璃、文字 `#f5f5f7` / `rgba(235,235,245,.6)`；**Element Plus 走 `html.dark` + `dark/css-vars.css`**，`--el-color-primary` 映射系统蓝，圆角 var 全部覆写为 8px
- 语义色：红=关闭/危险、绿=DB 正常、黄=最小化（红绿灯专用，不挪用）

### 2.4 字体与控件尺寸
- 字栈：`-apple-system, "SF Pro Text", "PingFang SC", "Microsoft YaHei", sans-serif`（无外链字体，纯系统栈）
- 最小字号 12px；可点控件 ≥ 32px（桌面场景），触屏 ≥44px；标题栏高 52px / 菜单栏 28px / Dock 76px
- 等宽（时钟/计数）：`"SF Mono", Consolas` + `tabular-nums`

### 2.5 动效（0.25–0.5s 弹簧）
```css
--spring: cubic-bezier(.34,1.3,.5,1);      /* 弹入：窗口/启动台/文件夹 */
--ease:   cubic-bezier(.25,.1,.25,1);      /* 常规过渡 */
窗口开 320ms scale(.96→1)+fade · 关 200ms fade+scale(.98) · 最小化 280ms 向 Dock 方向收束
红绿灯 hover 显形 120ms · Dock 放大插值 0.32/帧（保留原实现） · 右键菜单 140ms fade+4px 上移
prefers-reduced-motion: 全部退 opacity
```

### 2.6 投影（三层，参考图窗口质感）
```
--shadow-win: 0 0 0 .5px rgba(0,0,0,.18), 0 12px 28px -6px rgba(0,0,0,.28), 0 32px 64px -16px rgba(0,0,0,.22);
active 窗口加深一档，非 active 降饱和 + 玻璃变薄
```

### 2.7 壁纸（参考图同款「暖贝折叠绸」）
纯 CSS/SVG 重绘：`#c9b48f→#8f7a5c` 大曲率缎带（3 层 conic/radial 渐变 + blur(2px) + 轻噪点），深色模式换 `#2b2620→#141210` 黄昏调；视差保留原 `--px/--py`。

## 三、组件还原规格

### 3.1 菜单栏 `MenuBar.vue`（28px，glass-thin，通顶）
- 左：Logo（火箭）→ 系统菜单；**应用名加粗** + 应用菜单；文件/前往/显示/窗口/帮助（同一份 items 引擎，`CtxMenu.vue` 复用）
- 右状态簇（图4）：`进度库芯片（LED+已读x/总数·掌握x/总数）` · 外观按钮 · `x月x日 周x HH:MM`（点=关于本机）
- 菜单间悬停切换、键盘 ←→↑↓ Enter、Esc 关闭 —— 全部保留原行为

### 3.2 右键菜单 `CtxMenu.vue`（图4 规格）
- glass-thick、圆角 10px、内描边、item 高 30px、hover = 白 8% 叠加
- 条目类型：`{title}` 命名区（次级灰 13px）｜普通项（左 20px 图标槽）｜`checked` ✓ 槽｜`kbd` 右侧快捷键（灰 12px 等宽）｜`danger` 红｜`sep` 分隔线（0.5px，两侧 6px）｜子菜单箭头（本期只做「Services」样式的占位不实现二级）
- 定位贴边翻转、拖焦高亮、长按 0.5s 等四条入口 —— 与原版一致

### 3.3 程序坞 `Dock.vue`
- 居中玻璃长条 radius 22px，item 48px 基准、指针高斯放大 1.5/SPAN46（逐帧插值保留）、运行点 4px、悬停白字 tip、弹跳 0.6s
- 分隔线后：最小化窗口收纳区（图标 + 短标题）—— 原样保留

### 3.4 桌面图标 `DeskIcons.vue` + **启动台** `Launchpad.vue`（图1）
- 桌面图标：双击/Enter 打开，单击选中，右键菜单 —— 原样保留
- 启动台 overlay：`Ctrl+⇧A` / Dock 启动台图标 / 桌面右键进入；进入时壁纸 `blur(40px)+暗化12%`，320ms spring
  - 顶部胶囊搜索「搜索应用与章节」+ 右侧 ⋯（glass-med，280px）
  - 网格 7 列（≤1080px 5 列 / ≤760px 4 列）：小抄、题库、12 章瓷贴（章色 8% 纸染 + 编号）、题库文件夹瓷贴、启动台偏好
  - 输入即过滤（名称/编号），空态成句提示；Esc 退出、点击空白退出、页点仅装饰位
- **文件夹面板**（图2）：点题库文件夹 → 居中玻璃面板 radius 24px，标题「检查系统」+ 7×2 网格（14 系统，图标=apps.js 的 JBL_CHAPTER_ICONS）；点系统 = 打开/聚焦题库窗口并跳 `/jbl/chapter.html?c=…`

### 3.5 窗口 `WinFrame.vue`（图3/图4）
- 结构：标题栏 52px（glass-med）+ 主体（侧栏 + 内容）
- 标题栏：`红绿灯(悬停显形 ×−⤢, 12px)` → 侧栏钮/后退/前进 → **应用小图标 18px + 标题(当前标签页名) + 副标(应用名)** → 搜索钮(kbd ⌘K) + ⋯ 菜单
- 拖拽/8 向缩放/最大化/半屏 tile/两窗并列/最小化/Dock 收纳 —— 行为与原版逐一相同
- 侧栏：glass-thin、组标题 12px 次级色、行高 34px、当前项 = accent 12% 底 + accent 圆点；拖缝调宽 168–340 双击复位；底部进度条（45s 轮询 + 立即探测）
- 标签页：玻璃小卡 radius 8px、激活白 10% 叠加、拖拽排序、中键关、单标签收条 —— 原样
- **内容区两种宿主**：小抄 = 原生 `<NotebookRoot>`（无 iframe）；题库 = `<iframe>`（保留 desk-bridge 快捷键/主题上报）

### 3.6 ⌘K 面板 `Palette.vue` / Sheet 系列
- 面板：宽 640px、顶部玻璃输入行 + 范围分段（全部/小抄/题库）、分组列表（命令/已开标签/模块/最近/正文命中 `<mark>` 高亮+摘要）、底部键位提示行 —— 数据流与原版一致（150ms 防抖 /api/search）
- Sheet（确认/关于/偏好/快捷键/指南）：El-Dialog 深度换肤为 glass-thick 大圆角；偏好页用 `ElSegmented`（外观三档、侧栏宽度三档）+ `ElSwitch`（降低透明度）

### 3.7 小抄纸面系统（不玻璃化！）
- `cheat.css` → `nb.css`：`:root/html/body` → `.nb-scope`；夜读 `data-mode` 贴容器；宽度媒体查询 → `@container nb`；案面织纹/暗角合进多层 background；`--nb-h` 由 ResizeObserver 注入替代 `100vh`
- 三页（卡阵/章节/阅读）逐功能移植：tape/bento/折角/刻度三态/背完章/骨架卡；穿孔/波浪朱线 clip-path/分组统计/乐观回退/批量确认(ElMessageBox)；内容注入/图片降级/表格横滑壳/目录卡折叠+朱线进度/spy/逐节划掉联动/自愈/盖章/上下篇/导航轨/夜读/字号三档/mermaid 主题跟随
- URL 语义原样保留：`/index.html` `/chapter.html?c=` `/read/**` 由 vue-router 同名路由接住（Express SPA fallback 已就位），正文内链零改动

### 3.8 状态与主题
- `windows.js`：wins/order/front/zTop + 全部动作 + `desk:session` 200ms 防抖持久化 + 启动逐窗 90ms 恢复（**沿用原 localStorage 键，老会话无缝继承**）
- `prefs.js`：`desk:prefs`（theme auto/light/dark、opaque、sideW）+ 小抄 `read-mode/read-size`；外壳深浅色 ⇄ 小抄 day/night 联动规则照旧（auto 时应用内切换可反向跟随）
- 快捷键全套 + JBL iframe `desk-bridge` 回传 + 消息协议（ready/shortcut/mode）照搬

## 四、明天的实施顺序（预计一轮做完）

1. `styles/desk.css` 令牌 + `ep-mac.css`（EP 换肤）+ 壁纸
2. `lib/apps.js` `lib/icons.js` `lib/nburl.js` `api.js`
3. stores：`prefs` → `desk` → `windows` → `notebook`
4. 外壳组件：CtxMenu → MenuBar → Dock → DeskIcons → WinFrame → Palette → Sheets → DesktopShell
5. 小抄：nb.css 生成脚本（cheat.css 作用域化）→ NotebookRoot → Home → Chapter → Read → NotebookWindow/Page
6. 启动台 + 文件夹面板（P1）
7. `vite build` → 起服务 → 浏览器全清单验收（窗口拖缩/标签/搜索/主题联动/三页/深链/降级态）

## 五、验收清单

- [ ] 桌面：壁纸/菜单栏/Dock 放大/图标双击/右键菜单（桌面、窗口栏、标签、侧栏行四套）
- [ ] 窗口：开-关-最小-最大-还原-tile-并列-拖出还原；红绿灯悬停显形；会话恢复（含多标签历史）
- [ ] 侧栏：目录加载失败态/当前章高亮（含 /read 反查）/拖宽/进度条
- [ ] ⌘K：命令/模块/正文命中高亮/新标签打开/范围过滤
- [ ] 小抄三页全功能 + MySQL 缺席时 503 成句降级 + 夜读字号 + mermaid 换肤
- [ ] JBL：iframe 窗口/掌握进度/桥接快捷键
- [ ] 深链：`/read/java/basis/…` 直开、`/chapter.html?c=02`、`/index.html`
- [ ] 深浅色 + 降低透明度 + reduced-motion/transparency 降级
